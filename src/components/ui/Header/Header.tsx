// import banner from "../../../assets/header.jpg"
import s from "./Header.module.css"

const Header = () => {
  return (
    <div className={`${s.header} animate__animated animate__fadeInDown`}>
    </div>
  )
}

export default Header